// Empty stub so modules that `import "server-only"` can be unit-tested.
// In real builds Next.js intercepts this import and rejects use from a
// client component. In Vitest we don't care about that distinction.
export {};
